version: '3.8'

services:
  bot:
    build: .
    container_name: emoji-friends-bot
    restart: always
    env_file:
      - .env
    volumes:
      # Persist the database file on the host
      - ./data:/app/data
